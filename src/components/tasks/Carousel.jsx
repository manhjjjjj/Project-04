import React from 'react';
import Cards from './Cards';

const Carousels = () => {
    return (
        <>
            <Cards />
        </>
    );
}

export default Carousels;
// render(<ControlledCarousel />);